Here are the models scripts used to train the models.
They are, by default, configured so that they look for the data partitions in "<project_root>/data".
The path to this folder is hard coded, at the top of the file. 
